---
description: How to serve the static site and verify changes in Chrome
---

1. Start the static server:
// turbo
`shock serve`

2. Open the browser to verify changes:
Use the `open_browser_url` tool to navigate to `http://localhost:4000`.

3. Verify the content:
Use the browser subagent to check if the changes you made are reflected in the UI.
